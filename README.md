# Simpel Chat

Start the server with `cargo run <port>`. If `port` is not supplied, the server picks one that's available and logs it.

Netcat (`nc`) can be used to connect to the server with `nc localhost <port>`. 
